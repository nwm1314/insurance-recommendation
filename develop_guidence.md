# 智能保险推荐工具全栈开发指南

## 核心架构概览

本系统旨在通过“Python 自动化爬虫 + 大模型结构化解析 + 规则与 AI 双引擎推荐”，在不依赖任何商业保险 API 的前提下，打造一个高可用、智能化的保险推荐产品。

* **数据采集层：** Python + Playwright（应对前端异步渲染网页）。
* **数据清洗层：** 大模型 API（GPT-4o-mini 或同等模型）+ 强制 JSON 提取。
* **数据存储层：** SQLite（初期）/ PostgreSQL，基于 MD5 指纹进行增量维护。
* **推荐引擎层：** FastAPI/Flask，实现“规则树召回 + LLM 精排”双引擎。
* **网关防护层：** Redis 令牌桶限流 + 熔断降级机制。

---

## 第一阶段：构建高可用数据底座（自动化 Pipeline）

这是整个系统的基石，核心是将各大测评网或保司官网的非结构化网页文本，低成本地转化为强规则的本地数据库。

### 1. 数据库核心 Schema 设计

系统必须建立“四维数据表”以支撑后续的规则过滤与对比：

* **`products` (主表)：** `id`, `name`, `company`, `type` (重疾/医疗/意外/寿险), `status` (1在售/0停售)。
* **`rules` (规则表)：** `product_id`, `min_age`, `max_age`, `job_class_limit` (准入条件)。
* **`benefits` (责任表)：** `product_id`, `benefit_name`, `desc` (用于前端生成对比表)。
* **`page_logs` (日志表)：** `product_id`, `page_md5_hash` (用于监控产品是否暗中更新)。

### 2. 数据采集与 LLM 结构化

1. 利用 Playwright 定时抓取产品详情页的**基础信息、保障责任、投保规则**纯文本。
2. 通过构建严格的 System Prompt，调用大模型 API 并开启 `JSON Mode`，强制大模型按 `rules` 和 `benefits` 的表结构返回结构化数据，并存入数据库。

### 3. 数据自动维护与防雷（停售预警）

* **MD5 增量比对：** 每周运行后台巡检脚本，重抓 `status=1`（在售）的产品页面并计算核心文本 MD5。若与库中记录不符，触发大模型重新解析。
* **状态下架判定：** 爬虫检测到页面出现“已停售”、“暂不可投保”等 CSS 标签或关键词时，立即将数据库 `status` 变更为 0，阻断推荐。

---

## 第二阶段：双引擎推荐逻辑开发

引擎必须兼顾“金融合规的严谨性”**与**“AI的个性化体验”。任何情况下，大模型绝不能脱离规则树独立决策。

### 1. 基础漏斗：规则树粗筛（绝对防线）

当接收到用户画像（年龄、职业、收入、健康状态）时，必须先走纯代码 SQL 查询：

* **一票否决权：** 剔除所有不在年龄、职业允许范围内的产品，以及已停售产品。
* **预算控制：** 保费控制在年收入 5%-10% 内。极低预算（<3%）只推医疗+意外；高预算加入终身重疾。
* **角色控制：** 0-17 岁绝对不推寿险；55 岁以上绝对不推重疾险（替换为防癌险）。
* **结果输出：** 得到一个 10-20 款绝对合法合规的“安全候选池”。

### 2. 核心路由：双引擎分支

前端传入 `enable_llm_engine` 布尔值，后端进行路由：

* **极速规则模式 (False)：** 从“候选池”中按预设权重（如保费最低或保障最全）直接输出最终方案，1 秒内响应。
* **AI 专家模式 (True)：** 将“候选池”的数据与用户个性化偏好打包，作为 Prompt 发给大模型，由大模型挑选出最终 3-4 款产品，并撰写 200 字极具人情味的专属推荐语。

---

## 第三阶段：企业级高可用与防护网设计

由于引入了耗时较长的大模型调用，必须按行业标准做好防护，防止服务雪崩。

### 1. 流量控制（Rate Limiting）

* **IP 防护：** 限制 10次/分钟/IP。
* **用户防护：** 限制 3次/分钟/User，50次/天/User。触发限流返回 HTTP 429。

### 2. 超时与并发标准 (AsyncIO + SSE)

* **双向超时：** 后端调用大模型的建立连接超时设为 3 秒，读取结果超时设为 15-30 秒。前端请求等待超时设为 35 秒。
* **流式输出：** 强烈建议采用 Server-Sent Events (SSE) 技术，实现大模型推荐语的“打字机”效果，缓解用户等待焦虑。

### 3. 终极杀手锏：熔断与柔性降级 (Graceful Degradation)

* **机制逻辑：** 在 `try-except` 块中捕获所有 LLM API 异常（网络超时、并发被打满、网关 500 崩溃等）。
* **无缝降级：** 发生任何异常，**绝不向前端抛出错误**，而是立刻在后台将结果静默切回“极速规则模式”，并附带友好提示语（如：“AI 线路繁忙，已自动切换至极速专家推荐”），确保核心业务流程（拿到推荐方案）不中断。

---

## 第四阶段：前端交互与商业体验 (UX)

工具的最终成败在于前端体验，需重点实现以下模块：

1. **健康告知前置拦截：** 在首页问卷第一步，加入简易的异常勾选（如：是否住院、结节、三高）。有异常者，在结果页标红提示“该产品需走人工或智能核保”。
2. **引擎切换动画：** 为“极速模式”和“AI模式”设计优雅的 Switch 开关。开启 AI 时辅以“正在全网比对数据...”的动态安抚文案。
3. **直观的对比视图 (Table)：** 提供产品横向对比表格，高亮“保障差异”与“保费差异”。
4. **合规免责声明：** 页面显著位置需标明“本方案由算法生成，仅供参考，最终承保以保险公司官方条款为准”。