# 智能保险推荐工具全栈开发指南

## 核心架构概览

本文件是项目早期设计指南；实现状态以代码、测试和 `docs/tasks/2026-08-11-audit-task-cards.md` 为准。当前系统通过“Python 抓取 + OpenAI 兼容 LLM 结构化提取 + 规则引擎推荐”提供辅助信息，不依赖商业保险 API，也不提供实时核保报价。

* **数据采集层：** Python + Playwright（应对前端异步渲染网页）。
* **数据清洗层：** OpenAI-compatible 大模型 API + JSON 提取 + Pydantic 白名单/字段校验。
* **数据存储层：** SQLite（开发）/ PostgreSQL，原始文档使用 MD5 做内容未变化跳过。
* **推荐引擎层：** FastAPI + SQLAlchemy，实现“规则筛选 + 评分 + 套餐组包”；LLM 只解释规则引擎已选结果。
* **网关防护层：** Redis 令牌桶限流 + 熔断降级机制。

---

## 第一阶段：构建高可用数据底座（自动化 Pipeline）

这是整个系统的基石，核心是将各大测评网或保司官网的非结构化网页文本，低成本地转化为强规则的本地数据库。

### 1. 数据库核心 Schema 设计

系统必须建立“四维数据表”以支撑后续的规则过滤与对比：

* **`products` (主表)：** `id`, `name`, `company`, `type` (重疾/医疗/意外/寿险), `status` (1在售/0停售)。
* **`rules` (规则表)：** `product_id`, `min_age`, `max_age`, `job_class_limit` (准入条件)。
* **`benefits` (责任表)：** `product_id`, `benefit_name`, `desc` (用于前端生成对比表)。
* **采集审计模型：** `source_platforms`、`source_pages`、`crawl_jobs`、`crawl_runs`、`raw_documents`、`extraction_runs`、`product_drafts`、`product_versions`、`product_review_tasks`、`product_field_evidence`。

### 2. 数据采集与 LLM 结构化

1. 调度器按 `CRAWL_INTERVAL_MINUTES` 执行启用的抓取任务；抓取前校验 SSRF、robots 和重定向目标。
2. 对产品详情页提取**基础信息、保障责任、投保规则**，使用 OpenAI-compatible API 返回 JSON，再通过 Pydantic schema 校验。
3. 抓取结果先进入 `pending_review` 草稿和证据链，不直接进入推荐池；管理员审核后以事务方式发布 Product/Rule/Benefit/Version。

### 3. 数据自动维护与防雷（停售预警）

* **MD5 增量比对：** 每次任务抓取后计算文本 MD5；与最近原始文档相同则记录跳过，不创建重复原始文档和审核草稿。当前不是固定“每周”频率，间隔由环境变量配置。
* **状态下架判定：** 页面出现“已停售”“暂不可投保”等信号时生成 `off_shelf` 审核草稿；审核通过后停售并写入版本，阻断推荐。系统不会把未经审核的抓取结果直接改写为在售主数据。

---

## 第二阶段：双引擎推荐逻辑开发

引擎必须兼顾“金融合规的严谨性”**与**“AI的个性化体验”。任何情况下，大模型绝不能脱离规则树独立决策。

### 1. 基础漏斗：规则树粗筛（绝对防线）

当接收到用户画像（年龄、职业、收入、健康状态）时，必须先走纯代码 SQL 查询：

* **一票否决权：** 剔除所有不在年龄、职业允许范围内的产品，以及已停售产品。
* **预算控制：** 先按报价下限检查险种预算，再在组包时检查已知报价上限；缺失上限的产品必须标记“起/以核保为准”。
* **角色控制：** 0-17 岁绝对不推寿险；55 岁以上绝对不推重疾险（替换为防癌险）。
* **结果输出：** 得到一个 10-20 款绝对合法合规的“安全候选池”。

### 2. 核心路由：双引擎分支

前端传入 `enable_llm_engine` 布尔值，后端进行路由：

* **极速规则模式 (False)：** 从“候选池”中按预设权重（如保费最低或保障最全）直接输出最终方案，1 秒内响应。
* **AI 解释模式 (True)：** 将规则引擎已经组出的套餐与画像提示打包给大模型，生成受 Pydantic 和产品 ID 白名单约束的结构化解释。大模型不得新增、替换或独立选品。

---

## 第三阶段：企业级高可用与防护网设计

由于引入了耗时较长的大模型调用，必须按行业标准做好防护，防止服务雪崩。

### 1. 流量控制（Rate Limiting）

* **IP 防护：** 默认 120 次/分钟/IP（由 `RATE_LIMIT_IP_PER_MINUTE` 配置）。
* **用户防护：** 默认 30 次/分钟/User、300 次/天/User（由 `RATE_LIMIT_USER_PER_MINUTE` / `RATE_LIMIT_USER_PER_DAY` 配置）。Cookie 和 Bearer access token 均会解析用户 ID，触发限流返回 HTTP 429；Redis 不可用时当前实现放行。

### 2. 超时与并发标准

* **双向超时：** 后端调用大模型的建立连接默认 3 秒，读取结果默认 90 秒；通过 `LLM_CONNECT_TIMEOUT` / `LLM_READ_TIMEOUT` 配置。前端当前使用同步 JSON 请求。
* **流式输出：** 当前版本没有 SSE API、`EventSource` 或 `useSSE` hook；如未来实现流式输出，必须另立任务并补充断线、降级和验收测试。

### 3. 终极杀手锏：熔断与柔性降级 (Graceful Degradation)

* **机制逻辑：** 在 `try-except` 块中捕获所有 LLM API 异常（网络超时、并发被打满、网关 500 崩溃等）。
* **无缝降级：** 发生任何异常，**绝不向前端抛出错误**，而是立刻在后台将结果静默切回“极速规则模式”，并附带友好提示语（如：“AI 线路繁忙，已自动切换至极速专家推荐”），确保核心业务流程（拿到推荐方案）不中断。

---

## 第四阶段：前端交互与商业体验 (UX)

工具的最终成败在于前端体验，需重点实现以下模块：

1. **健康告知提示：** 当前后端识别有限的健康问题别名并输出匹配/风险提示；未知问题不会被当作已核保。结果页必须提示用户以健康告知和保险公司核保为准。
2. **引擎切换动画：** 为“极速模式”和“AI模式”设计优雅的 Switch 开关。开启 AI 时辅以“正在全网比对数据...”的动态安抚文案。
3. **直观的对比视图 (Table)：** 提供产品横向对比表格，高亮“保障差异”与“保费差异”。
4. **合规免责声明：** 页面显著位置需标明“本方案由算法生成，仅供参考，最终承保以保险公司官方条款为准”。
